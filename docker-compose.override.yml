services:
  web:
    build:
      context: .
      dockerfile: docker/Dockerfile.web
    image: instance-starter-local

  celery_worker:
    build:
      context: .
      dockerfile: docker/Dockerfile.web
    image: instance-starter-local

  celery_beat:
    build:
      context: .
      dockerfile: docker/Dockerfile.web
    image: instance-starter-local